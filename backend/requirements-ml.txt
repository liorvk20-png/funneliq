# Training and analysis only — deliberately NOT in requirements.txt.
#
# These four add roughly 250 MB to an environment. The web service does not
# need them yet, and every megabyte here is build time and credit spent on
# every deploy. They move into requirements.txt at the point the API actually
# serves a prediction, and that deploy needs verifying when it happens rather
# than assumed.
#
# Install:  pip install -r requirements-ml.txt
scikit-learn==1.9.0
xgboost==3.4.1
lightgbm==4.7.0
catboost==1.2.10
